<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sarker.ereg">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/reg_icon"
        android:label="eRegistration"
        android:roundIcon="@drawable/reg_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditDetails"
            android:theme="@style/Theme.MyApp"/>
        <activity
            android:name=".AddStudent"
            android:theme="@style/Theme.MyApp" />
        <activity android:name=".StudentDetails" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ViewStudentList" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>